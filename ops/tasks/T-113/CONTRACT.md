# T-113 contract: independently integrate retained T-112 FAIL

Act only as the evidence Integrator for T-112 candidate
`c98498696de64e4f8aa99509e2dbad3d1a6fd1b6`, sole parent
`4277111312b09d549b29b0e3af8c0e4ebcef2168`, tree
`6059e88d2f58c9c1632f2ff2194f47618ef6fb75`, and stable patch ID
`e58936f2c2016aa590e6825748558c37fdf302d2`. Start only from PB-000's exact
committed T-113 binding. Require exactly two candidate paths and blobs:

- `2d86d87340661d9d758ee366a2712e77c96ca0cd` T-112 receipt;
- `6e4644bd668902a771d5cace8497dac1bad23f53` T-112 handoff.

Replay those blobs byte-identically onto the committed saved lineage. Do not
merge the runtime worker branch, rewrite evidence, or change source/tests.

Treat `D:\Codex-Labs\evidence\T-112\one-zone-autonomy-v9` as immutable and
read-only. Require manifest SHA-256
`b9a11a357e108a05f203ab5a43cd9f3ea9c83f1573748e20b1209b1352699b01`
and terminal-result SHA-256
`a6374a1be0338341681f00ae8c5f58c201e3397d59d6f2479dc05408e87935ef`.
Independently validate canonical relative-path safety and uniqueness, rehash
all 8,602 listed payloads and 29,445,543 bytes, reject reparse points, and
require zero missing, mismatched, duplicate, unsafe, or unlisted payloads. Do
not write an audit file into the root.

Independently parse the preserved T-110 named-grade selftest response and
negative fixtures and require the exact grammar result. Independently replay or
mirror both retained planner algorithms rather than trusting derived summaries.
Require the 18,399,744-row retained-bit selftest to reproduce 125 eligible,
semantic SHA-256
`38b871081b5c3dc4be700d33f9890c51e10aa3b1c80839b8b916584fa553bfc1`,
orders `28/68/56`, exact fixture bits and extrema, and zero persisted rows.
Verify the two BelowNormal PIDs, 8,192 yield interval, AboveNormal sentinel,
68 samples, and maximum gap `533.132 ms` from raw rehearsal timestamps.

Independently replay Director timestamps and require first admission at
`180068.861 ms`, order `[20001,20002,20003]`, gaps
`15021.616/15044.714 ms`, and zero failure/wrong-zone/overlap. Recompute the
live dual-planner result from the exact retained transform bits: 18,399,744
tuples, 4,491 eligible, semantic SHA-256
`a20d960918bae85143bf81d9387049b17214238bb4708005f94ffb49315aa222`,
orders `259/242/250`, distances `55/55/55`, angles `-75/+30/+150`, matching
primary/repeat, and no persisted rows. Revalidate post-plan bit/state identity,
observer maximum gap through command `591.089 ms`, batch start lag
`928.269 ms`, response span `184.973 ms`, unique objects
`33901/37936/37938`, three `Weak` grades, predicted/actual matrices, and zero
parser, command, transform, geometry, retry, or directed-gameplay errors.

Independently rehash and parse every raw sample referenced by the lifecycle
validator. Require one normal logout per identity and Director refill order
`20001/20002/20003`, but reproduce the fixed-counter failure exactly:

- 20001: 16 pending rows, brain `177/178/179`, mover `433/434/435/436`;
- 20002: 11 pending rows, fixed brain/mover `179/436`;
- 20003: 13 pending rows, brain `178/179`, mover `435/436`.

Confirm original error `fixed-counter-window-invalid:20001:16`, failed bot IDs
`20001/20003`, no wave two, no restart, and no retry. Successful fixture
placement and refill readiness must not be reinterpreted as lifecycle PASS.

Rehash and recompute all 1,315 client samples and chain links, require zero
client/error samples, gap `728.458 ms`, PID `129400`, no relaunch, cooperative
stop, and terminal chain
`99b33ba8083c20e42913b2437836ba729b2795c982cea6da19ac1d9f2f1095bb`.
Independently rederive the `778/779/779` valid autonomy responses with zero
malformed rows and reproduce complete-ledger shutdown-tail maximum gaps
`2582.087/2597.298/2569.992 ms`, each above 2,000 ms. Keep that observer gap
failure distinct from the earlier fixed-counter behavior failure.

Verify exact graceful Game-then-Login shutdown, six despawns, cooperative
observer exits, byte-exact config restoration SHA-256
`7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202`,
zero final runtime/client/observer processes, required listeners and live logs,
unchanged OS-observed MySQL PIDs `6308/8076`, exact clean reference/module and
unchanged 30-entry host overlay, zero database/client access, and no force-stop.
Retain every versioned tooling, ledger-sharing, audit-wrapper, and no-op first
shutdown anomaly without treating it as evidence mutation.

After every independent gate passes, commit only the T-113 integration receipt
and handoff after the exact two-blob replay, then fast-forward saved branch
`integration/aaemu12-world` through exactly those two commits without history
rewrite. Report all identities, manifest, parser/planners, bootstrap, wave-one
fixtures, raw counter failure, observers, cleanup, anomalies, and unproven
boundaries. Verdict remains FAIL; T-037 stays blocked. Do not start runtime or
prepare a source fix. PB-000 alone may bind the next source task.
