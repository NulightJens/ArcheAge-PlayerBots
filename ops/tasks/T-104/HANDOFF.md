# T-104 handoff

Verdict: **FAIL**.

T-104 ran from binding commit `dde605d10912e8f282ce263680b70d1f02133249`, tree `b78381d302b83a0968267e40b8a4f07a328e2290`, dispatch base `dc9a1f721e1cb1e972a3a1d8c58a51f3c5add056`, and task `01a05f1e-79b2-7cd1-beef-446c870c4f9a` under the exact committed `aaemu12` lease.

Preflight passed against AAEmu host `62e3eb1d87da01194802ac886cd500134facad28`, module source/tree `bf0ae36fc65eea4f341b936c9f7a961e9d474580` / `7f33545311c972d7a0ed31fa7f75a016df3571f7`, patch SHA-256 `baa0979a200eb7f4403985d7166092f937033c31650aa11336fba31543d96606`, and Game assembly SHA-256 `5a2b9e9a32163b6003c0d4feb05fb68a06086bc23b6a7a6d2bfdc455d07942f2`. The reference and installed module were clean and detached, the expected 30-entry host overlay was present, all required ports and live-log paths were clear, no runtime/client/observer process existed, and OS-only observation found unchanged MySQL PIDs `6308` and `8076` without database access or control.

The nine Director assignments were deployed exactly. Config SHA-256 changed from `7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202` to `5177e67c818d04ebf9c6b0164afc833e431cf4f687e660cffe02d5ef202de47c`, preserved search radius `60`, and was restored byte-for-byte after shutdown.

The Windows PowerShell client observer launched once as PID `117976` before Login. Independent final replay validated 1,345 raw snapshots and 1,345 chain rows, zero sequence/snapshot mismatches, zero client or error samples, maximum adjacent gap `802.382 ms`, and terminal chain hash `6ad451b3e43d52a18f684e94862b4c13fa07482edee599addd2fead6dd826a64`. Its first sample was `2026-09-01T22:55:58.3786962Z`, its last was `2026-09-01T23:08:33.7223742Z`, and it exited through the fresh cooperative sentinel after config restoration.

Run one used Login PID `108516` and Game PID `124496`. The exact autonomy observers launched once as PIDs `117840`, `121500`, and `116032`. Their first samples arrived `91609.648`, `91646.631`, and `91627.075 ms` after runtime-start evidence. All had two raw-verified offline samples plus armed/live boundaries before admission. Final replay validated 5,357 raw responses, all derived/transport indices, and zero duplicate or unreferenced raws. Their stable online object IDs were `44939`, `7797`, and `37939`.

The Director bootstrap passed: start `2026-09-01T22:57:16.0947035Z`, first admission `2026-09-01T23:00:16.1370210Z`, observed delay `180042.318 ms` within `[180000,195000]`, ordered admissions `[20001,20002,20003]`, reconciliation gaps `15021.655` and `15051.77 ms`, maximum three, and zero failures, wrong-zone activations, in-flight overlap, or population breach.

At the only contracted fixture boundary, two stable raw samples per identity spanned `530.307 ms`; the latest was `183.176 ms` old. Exact latest transforms were `(13614.2,13307.8,28.9379,-3.93849)`, `(13622.9,13316.3,28.8911,-3.94029)`, and `(13627.5,13320.5,28.8686,-3.9466)` in world/instance/zone `0/0/221`. The complete 1,331-tuple enumeration is 826,258 bytes with SHA-256 `42e65f21c8d5dce3980f0dd55bc9c98b10405bdfc4ce81b994aaa358b7b7d788`.

All 1,331 tuples were finite and within the 60 m scan radius. Eighty-seven met the 20 m predicted pairwise separation threshold, but zero met the required 3 m own-versus-foreign margin. The best achievable minimum margin was `-1.2237695385452456 m`; its offsets were `[5,5,10]`, with pairwise distances `[12.16378785829667,13.390419351963843,1.23379116495982]`. The best pairwise tuple `[55,35,5]` had minimum pairwise distance `32.16448164882895 m` but ownership margin `-41.61124645561213 m`. There is therefore no selected tuple or selected-plan hash. The contract required safe stop, and no fixture request, response, spawn, directed gameplay command, retry, or world mutation occurred.

Game then Login stopped by graceful Ctrl+C. The Director stopped once, all three bots despawned, and the once-only cleanup marker recorded zero remaining bots and runtimes. The three autonomy observers ended cooperatively; the client observer ended through its sentinel. Run-one logs moved losslessly. Final checks prove zero runtime/client/task/observer processes, zero required listeners and live logs, unchanged MySQL identities, unchanged exact source and host overlay, and byte-exact config restoration. `Error.log` retains the native post-shutdown PhysicsManager disposed-service-provider warning; all cleanup gates remain green.

The immutable evidence root is `D:\Codex-Labs\evidence\T-104\one-zone-autonomy-v6`. Manifest `manifest.json` covers 17,528 payloads and 40,239,358 bytes, is 2,753,724 bytes, and has SHA-256 `cbe9026f8e27ac0503521370a84be286b01ca38935cc8e3da4552e40bc31f7a0`. Independent replay found zero missing, length/hash-mismatched, duplicate, unsafe, unlisted, or reparse payloads.

Retained procedural anomalies are fail-closed pre-output PowerShell probes, two shell-policy-rejected inline observer launches before the three once-only PTY launches, and evidence-local nested BotDebug parser generations. The invariant transform parser and complete planner passed pre-runtime self-tests; the corrected nested wrapper was created after runtime start, and its superseded zero-command attempts remain retained. No anomaly issued a fixture or gameplay command.

Unproven boundaries are both lifecycle waves and refills, per-bot selection/navigation/combat/credit/progression/recovery/logout, selected and actual fixture geometry, distinct-PID restart, second delay measurement, ordered rebootstrap, two-minute dwell, scale, soak, packaging, client gameplay, database behavior, and AAEmu 3.0.

Integration action: independently verify the sealed root and integrate this commit only as the retained T-104 `FAIL` receipt. Keep T-037 blocked. PB-000 alone may release or reassign the lease after accepting cleanup. Any successor requires a new task, exact binding/lease, and a fresh versioned evidence root; never reuse v6.
