# T-094 handoff

Verdict: **FAIL**.

T-094 ran from binding commit `cc4f9fc6889abc17a149e6dad9d4ff92491a8dc0`, dispatch base `e70eb8d195a209ccef1c753ac48c6cc01fa89b86`, task `01a05e46-6402-7da0-b59e-7c757b6d0c68`, client token `client-new-thread:339575d7-83e5-488c-abfb-7fe729feba00`, and worktree `C:\Users\jensh\.codex\worktrees\2ba4\PB-W00-control` under the exact committed `aaemu12` lease.

The source/config/fresh-root preflight passed. The reference was clean and detached at `62e3eb1d87da01194802ac886cd500134facad28`; the installed module was clean at source `e284c739ae168fc95fb77a91bf0f23bd5dd2f6a4`, tree `4b3f96aaedb96ec40c1dc5eef4256efe02cd99f2`; and the compatibility patch, Game assembly, integrated client observer, and autonomy observer identities matched. The five required ports and live-log directories were empty, no AAEmu or ArcheAge client process existed, and OS-only observation found MySQL PIDs `6308` and `8076` without database connection or control. The fresh v4 root was absent and created exactly once.

The integrated Windows PowerShell client-process observer launched once as PID `22748` before Login. Raw sample `000000` and ledger row zero were independently hash-validated at process count zero. The final summary and independent replay validate 1,001 raw files and 1,001 chained rows, zero client-process samples, zero errors, maximum adjacent gap `650.484 ms`, first sample `2026-09-01T18:56:08.1843772Z`, last sample `2026-09-01T19:05:20.3607648Z`, and terminal hash `f84262166440f2fcd31d57984a0271d5bb53fc9815a5e165ab3f7c4fd9047b86`. The last sample follows final cleanup. The observer exited through its cooperative sentinel and was never relaunched.

The deployed Director config changed only the nine contracted assignments. Original SHA-256 `7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202` became `5177e67c818d04ebf9c6b0164afc833e431cf4f687e660cffe02d5ef202de47c`, with initial delay `180000` ms, then was restored byte-for-byte to the original hash after shutdown.

Run one used Login PID `96808` and Game PID `61536`. All three unchanged autonomy observers began about 11.4 seconds after runtime-start evidence and each durably published two offline samples with `online=false`, null object IDs, exact raw hashes, and armed/live boundaries before the first admission. The widened pre-arm boundary therefore passed: the latest second offline sample was `18:58:30.7703112Z`, while first admission was `18:59:11.1494980Z`.

The first material invalid gate was the exact Director delay within the zero-to-three bootstrap. The Director started once at `18:58:11.1285011Z`, but admitted bot `20001` at `18:59:11.1494980Z`: `60020.997 ms`, not the contracted and deployed `180000 ms`. It subsequently admitted `[20001,20002,20003]` in order with three successes and zero reported failures, but the delay mismatch is non-PASS. Execution stopped without retry.

No `spawnpassive`, directed gameplay, metrics, or roster command was issued. No fixture coordinates or pairwise separations were collected. Neither three-bot lifecycle wave, refill, per-bot credit/progression/recovery/logout proof, distinct-PID restart, rebootstrap, nor two-minute dwell was attempted.

Game then Login stopped gracefully; the Director stopped once; all three bots despawned; and the once-only cleanup marker reports zero remaining bots and runtimes. The autonomy observers ended by console cancellation. Run-one logs moved losslessly. Final checks prove zero runtime, autonomy-observer, client-observer, and ArcheAge client processes; zero required listeners and live logs; unchanged MySQL PIDs; exact source/assembly/config; and the expected 30-entry host overlay. The retained Game error log contains the native shutdown `PhysicsManager` disposed-service-provider warning; cleanup still passed.

The immutable evidence root is `D:\Codex-Labs\evidence\T-094\one-zone-autonomy-v4`. Its manifest covers 3,244 payloads and 24,226,957 bytes with SHA-256 `46ce346de698964231a522b2e3646af7a7fc0b0fbd5a080959c9e78da7d57293`; validation found zero missing, mismatched, duplicate, unsafe, unlisted, or reparse-point payloads.

Retained procedural anomalies are limited to non-mutating preflight/recording probes and correction of the autonomy validator's expected post-shutdown transport-error handling; they did not alter immutable observer outputs. Unproven boundaries are the exact 180-second delay, all fixture/separation and lifecycle-wave outcomes, both refills, restart/dwell, scale, soak, packaging, client gameplay, database behavior, and AAEmu 3.0.

Integration action: independently verify the sealed root and integrate this commit only as the retained T-094 `FAIL` receipt. Keep T-037 blocked. PB-000 alone may release or reassign the lease after accepting cleanup; any successor requires a new task, exact binding/lease, and a new versioned evidence root.
