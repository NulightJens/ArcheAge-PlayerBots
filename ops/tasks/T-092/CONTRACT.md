# T-092 contract: one-zone autonomy reproof with integrated client observer

## Outcome and fixed identity

Produce one fresh immutable live-headless AAEmu 1.2 receipt proving continuous
zero-client observation; exact Activity Director admission in zone `221`; two
independently credited, progressed, recovered, normally logged-out lifecycles
for each configured bot; complete refills; and a clean distinct-PID restart
that reboots and holds target. Retained T-041 remains FAIL and T-088 remains
INCOMPLETE; this task neither overwrites nor reinterprets them.

Use only registered workspaces `playerbots_control`, `aaemu12_reference`,
`aaemu12_integration`, `aaemu12_database_public_alpha_v1`, and
`aaemu12_t092_evidence_v3`. Require host
`62e3eb1d87da01194802ac886cd500134facad28`, installed module source/tree
`e284c739ae168fc95fb77a91bf0f23bd5dd2f6a4` /
`4b3f96aaedb96ec40c1dc5eef4256efe02cd99f2`, patch SHA-256
`baa0979a200eb7f4403985d7166092f937033c31650aa11336fba31543d96606`,
Game assembly SHA-256
`a4508402e8564a6c6807471c9cd6c9e9bc5950e279ea1a9e7841d35a61d35075`,
and integrated observer blob `0f8b5e2b3a811877648020b715e4737777d4947a`.

Use persistent IDs `[20001,20002,20003]`, zone `221`, and population bounds
minimum `2`, target `3`, maximum `3`. Change only the nine deployed Director
assignments: enabled; fixed zone, IDs and bounds; initial delay `60000` ms;
reconciliation `15000` ms; retry `30000` ms. Preserve exact original config
bytes, require continuity SHA-256
`7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202`,
and restore those bytes after final shutdown.

The fresh root is `D:\Codex-Labs\evidence\T-092\one-zone-autonomy-v3`.
It must be absent before its one-time creation. If present, stop without
overwrite, deletion, rename, or replacement. All predecessor roots are
immutable and read-only.

## Authority and prohibitions

Do nothing until the committed task/thread/worktree binding and exact aaemu12
lease agree. The lease may permit only loopback Login/Game start and graceful
Game-then-Login stop; the exact config edit/restore; read-only observers,
metrics/debug/roster queries; the integrated client-process observer; six exact
fixture commands below; normal runtime outputs; and the fresh evidence root.

Never start, stop, query, or control MySQL or a database. Observe existing
MySQL PIDs only. Never start or control the ArcheAge client. Never issue
`addbot`, `removebot`, `botstate`, activity/destination/target/travel/attack/
loot/progression/recovery/logout, metrics reset/activity, or any directed
gameplay command. Never edit source/tests/tools/scripts/manifests/patches,
global ledgers/lease, predecessor evidence, client files, or AAEmu 3.0. Never
delete/reset/clean/uninstall/force-stop. Preserve pre-existing logs losslessly.

## Preflight and continuous client observer

Retain exact binding/source/observer/config/schema-name/process/port/log/MySQL-
PID fingerprints and prove clean accepted reference/module/host state. Create
the fresh root once. Launch the integrated observer before Login with Windows
PowerShell and no profile, using its defaults and exactly:

`powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File <bound-worktree>\scripts\autonomy\Observe-ClientProcessAbsence.ps1 -OutputPath <fresh-root>\client-observer -StopSentinel <fresh-root>\client-observer.stop -SampleIntervalMilliseconds 500`

Retain launcher identity and source hash. Before Login, require `ready.json`, raw
sample `000000`, and ledger row zero; independently recompute raw and row hashes
and require process count zero. A child exit, `error.json`, missing/mismatched
row, or readiness before durable row zero is the first material invalid gate;
do not relaunch. The observer must remain alive through both runs and cleanup.

Start Login then Game through the accepted scale runtime script with isolated
public-alpha schema names and acknowledgement. Before the Director's 60-second
delay, start one unchanged autonomy observer per fixed bot. Each must retain raw
transport/responses/hashes/derived samples and prove two live offline samples
before first admission. Admission first is non-PASS.

## Director and two counted waves

Retain frequent `botmetrics snapshot` envelopes and authoritative logs. Require
once-only Director start, valid config, zero-to-exact-three admission in order
`[20001,20002,20003]` one per tick, zone `221`/default world/default instance,
maximum `3`, and zero failure, wrong-zone activation, overlap, or directed
command.

At each fresh all-three targetless, noncombat, stationary, full-resource
boundary, issue this exact three-command batch once and in order:

1. `spawnpassive 10004 5 20001`
2. `spawnpassive 10004 5 20002`
3. `spawnpassive 10004 55 20003`

Preserve exact request/response bytes. Require expected anchor ID, zone `221`,
common default instance, and finite position in each success. Derive all three
pairwise horizontal fixture distances before counting gameplay; every distance
must be at least `15.0` meters. On command/audit/separation failure, stop without
retry or extra fixture.

The batch is shared inert world input, not a target assignment. In each wave,
each configured identity must independently select `grind/nearby_mortal`,
navigate, cast/damage, receive exactly one native credit for one distinct batch
object, gain positive authoritative progression, complete ordered recovery with
fixed brain/mover counters while pending, record one debt-free completion, and
normally self-logout. No identity may receive two credits in one wave and no
dead/uncredited object counts. Prove exact Director refill to target after wave
one. Repeat the same batch at the next fresh safe boundary and require the same
one-credit lifecycle plus a second complete refill. Total fixture commands must
equal six; credited objects six; credits exactly two per identity. Stop at the
first invalid gate and never retry.

Reconcile observer samples, Director envelopes, metrics, progression, fixture
audits, command bytes, logs, and continuous client rows. Require zero tick
errors, maximum breach, cross-zone activation, forced transition, uncredited
completion, response/hash defect, or client process.

## Restart, cleanup, and sealing

After wave two and second refill, stop Game then Login gracefully. Prove zero
Game/Login/client processes, free required ports, once-only cleanup, and zero
bots/runtimes. Move run-one logs losslessly. Restart with distinct Login/Game
PIDs under the same config. Before admission, pre-arm three fresh autonomy
observers with two offline samples each. Prove normalized roster identity,
second zero-to-three ordered bootstrap, zone/default boundary, maximum `3`, and
at least two continuous minutes at reconciled target without fixtures or
gameplay commands.

Stop Game then Login gracefully. If graceful stop fails, leave the process
running, retain evidence, and report; never force-stop. Prove zero bots,
runtimes, Game/Login/client/autonomy-observer processes, listeners and live
logs; unchanged observed MySQL PIDs; exact source/host hashes; and exact config
restoration.

Create the stop sentinel as a new file, wait for the integrated client observer
to exit normally, and retain `summary.json`. Independently validate every raw
SHA and row-chain link; sample count equals raw/row counts; first sample precedes
Login; last sample follows final cleanup; maximum adjacent gap is at most 2000
ms; nonzero and error counts are zero; terminal chain hash matches; and no
`error.json` exists.

Seal the root with a complete relative-path/length/SHA-256 manifest and require
zero missing, mismatched, duplicate, unsafe, or unlisted payloads. Commit only
`ops/evidence/aaemu12-t092-one-zone-autonomy-v3.yaml` and
`ops/tasks/T-092/HANDOFF.md`. Report PASS/FAIL/INCOMPLETE with exact source,
lease, observer, config, processes, fixture coordinates/separations, per-wave
credits/progression/recovery/logout/refill, restart/dwell, manifest, cleanup,
anomalies and unproven boundaries. Do not edit ledgers or release the lease;
PB-000 alone dispatches independent evidence integration.
