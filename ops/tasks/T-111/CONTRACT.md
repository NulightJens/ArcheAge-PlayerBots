# T-111 contract: independently integrate retained T-110 INCOMPLETE

Act only as the evidence Integrator for T-110 candidate
`1b914085d64b3957b3c394d9b9f75600c91e2a96`, sole parent
`80bb3b9f3dc028e2b1830ef10421eed4dc13f4e1`, tree
`03d3e8fcda1ae196023c9acd412e212770940261`, and stable patch ID
`211ac120413c1c483c1ad9892afa08eecc9af3a9`. Start only from PB-000's exact
committed T-111 binding. Require exactly two candidate paths and blobs:

- `eed5dbb70e84ff2421ada3b54b4085b216526ebc` T-110 receipt;
- `dd362f075e06119293cb6e20f3eb37107dcb7e83` T-110 handoff.

Replay those blobs byte-identically onto the committed saved lineage. Do not
merge the runtime worker branch, rewrite evidence, or change source/tests.

Treat `D:\Codex-Labs\evidence\T-110\one-zone-autonomy-v8` as immutable and
read-only. Require manifest SHA-256
`b8678f4526f80197a0b659c9f22b660b686b3ed75731c3f57095123aedc943e1`
and terminal-result SHA-256
`bd29e1151367d88bd7df033dea51d88d4bec442ce3cdd54a1631afa5aab38a78`.
Independently validate canonical relative-path safety and uniqueness, rehash
all 5,918 listed payloads and 25,649,977 bytes, reject reparse points, and
require zero missing, mismatched, duplicate, unsafe, or unlisted payloads. Do
not write an audit file into the root.

Independently compile or mirror the retained designated planner algorithms and
decode the exact retained T-104/live transform bits. Recompute the complete
ordered 15-degree grid of 18,399,744 tuples with the literal bot-row diagonal
ownership criterion. Require exactly 125 eligible, semantic SHA-256
`38b871081b5c3dc4be700d33f9890c51e10aa3b1c80839b8b916584fa553bfc1`,
choice orders `[28,68,56]`, distances `[10,15,15]`, angles
`[60,-60,120]`, expected fixture float bits, minimum pairwise
`23.728951412717223`, minimum row margin `3.820467349324412`, independent
repeat equality, and zero persisted enumeration rows.

Independently replay Director timestamps and require first admission at
`180038.044 ms`, order `[20001,20002,20003]`, reconciliation gaps
`15049.859/15058.018 ms`, and zero failure/wrong-zone/overlap. Rehash and
recompute all 1,039 client samples and chain links, require zero client/error
samples, maximum gap `719.196 ms`, original observer PID `127276`, no relaunch,
and terminal chain
`b106ad863e3b33b018d2e50041ba8885a842dd24e2a61e71c31d68dc4702dc7b`.
Independently rederive all run-one autonomy raw samples `524/525/525`, require
zero malformed samples, and reproduce maximum adjacent gaps
`2575.486/2565.165/2565.218 ms`, all above the contracted 2,000 ms ceiling.

Require exactly one fixture request and response, no retry, and no later
fixture command. Rehash request/response SHA-256
`bd65dc9952049bf75e4610852cbfaade425e0714bc895769f11bb76766b314da` /
`32474d410485ba1fe987a80a0ed3b41b86f3729ee4fe3b30263f593e4de844d3`.
Independently parse the preserved 359-byte response and require object 44979,
anchor bot 20001, zone/instance `221/0`, finite rounded coordinates
`13616.7/13298.1/28.5`, and authoritative named `grade=Weak`. Confirm that the
retained local numeric-grade requirement—not server/source behavior—caused
the stop, while preserving INCOMPLETE because commands two/three, actual
three-fixture geometry, lifecycle waves, restart, and dwell are absent.

Verify exact graceful Game-then-Login shutdown, cooperative observer exit,
byte-exact 1,772-byte config restoration SHA-256
`7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202`,
zero final runtime/client/observer processes, required listeners and live logs,
unchanged OS-observed MySQL PIDs `6308/8076`, exact clean reference/module and
unchanged 30-entry host overlay, zero database/client access, and no force-stop.
Retain every superseded-tooling, observer-wrapper, audit-wrapper, and early
snapshot anomaly without treating it as external mutation.

After every independent gate passes, commit only the T-111 integration receipt
and handoff after the exact two-blob replay, then fast-forward saved branch
`integration/aaemu12-world` through exactly those two commits without history
rewrite. Report replay/final identities, manifest, planners, bootstrap,
observers, response, cleanup, anomalies, and unproven boundaries. Verdict
remains INCOMPLETE; T-037 stays blocked. Do not start runtime or prepare v9.
PB-000 alone may bind the fresh parser- and observer-safe successor.
