# T-072 contract

## Outcome

The accepted T-071 targetless/noncombat natural recovery correction is
independently replayed, installed in the registered AAEmu 1.2 host under an
exact build-only lease, and qualified by a clean full build and exactly one
complete unit-suite invocation before another physical one-bot attempt.

## Authority and pass

- Do not write the lease-controlled integration host/module or build outputs
  until PB-000 commits `ops/RUNTIME-LEASE.yaml` assigning `aaemu12` to T-072's
  exact thread for install/build/test only. The authority never permits Login,
  GameServer, MySQL, client, database, or live runtime control.
- Start from exact integration head
  `d71a52b10e78be9ca6a91a3b1d5822a5ef199740`. Verify candidate
  `16e8c2fe707f7bd96e4b6977dd6b7c441124c63f` with parent
  `a1c15016e3e7ccc121c0bad048dc7bb9d5c7aa8f`; both worktrees must be clean.
- Review and replay only the five source/test paths and T-071 handoff. Preserve
  controller-owned recovery pending/completed observations, brain/mover
  suspension, world-native regeneration, targetless/noncombat/movement-clear
  gates, invalid-resource fail-closed behavior, exact completion snapshot,
  single structured progression record, and unchanged logout callback.
- Retain and report T-071's pre-clarification registered-host output-only build
  event. Independently prove it changed no tracked host source/configuration or
  installed module identity before normal integration mutation.
- Verify pinned reference and registered host/module identities before mutation.
  Keep the compatibility patch byte-identical at SHA-256
  `0285c8c21133ec00abbd8f5de925c56dc2d87f7d8595fee89a6063b3ff02ca3e`.
- Advance the installed module non-destructively; run installer idempotence,
  directly affected tests, a complete AAEmu 1.2 solution build, and the full
  AAEmu 1.2 unit suite exactly once. Zero build/test failures are required;
  report only intentional skips.
- Recheck clean reference/module, unchanged tracked host overlay outside the
  receipted module state, zero Login/Game/client processes, and free ports.
- Commit only replayed paths, T-071 handoff, a sanitized integration receipt,
  and T-072 handoff. Fast-forward the saved branch through the qualified commit
  while preserving ledger-only descendants. State exact tested source/tree and
  whether a fresh immutable runtime proof may proceed.

## Non-goals

Any runtime/server/client/MySQL/database control; gameplay; source changes
beyond accepted T-071 blobs; retained evidence mutation; direct recovery;
Activity Director; scale; soak; packaging; AAEmu 3.0; or global ledger edits.
