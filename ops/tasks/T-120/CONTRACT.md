# T-120 contract: integrate and launch autonomous quest intake

Start runtime mutation only after T-118 has closed the ArcheAge client and
launcher normally, stopped Game then Login gracefully, restored the exact
original deployed `BotConfig.json` bytes, and reported zero relevant processes
and ports. PB-000 must then commit the T-120 lease transfer. Never infer lease
ownership from a quiet process or elapsed time.

Verify T-119 implementation commit
`2d1b8d7103d35c46ec1bc9f13df36e40f4ba2b15`, sole parent
`d42236e77cfc8030b78eb9750db9f78a3df8d35d`, tree
`1276326b17cabfb3dbe10010ddbcf65dea729417`, and stable patch ID
`d525d7bc14e72ac0c5042bc6443044762e253084`. Import the exact T-119 handoff
from its clean writer worktree. Replay the implementation onto the saved
integration branch without rewriting history and prove the replay has the same
stable patch ID and exactly the eleven contracted implementation paths.

Require the registered reference clean/detached at
`62e3eb1d87da01194802ac886cd500134facad28`, the existing 30-entry integration
host overlay, and compatibility patch SHA-256
`baa0979a200eb7f4403985d7166092f937033c31650aa11336fba31543d96606`.
Move only the clean installed module to the exact replay commit through normal
Git operations. Run the complete quest-intake controller, host, and command
test fixtures plus every non-destructive config test used by T-119. Then run
one registered AAEmu.Game no-incremental build; require zero errors and retain
the exact warning count and output assembly hash. Do not clean the host or run
the full suite.

Before editing deployed configuration, retain its exact bytes at a new T-120
session-local path. Enable `QuestIntakeEnabled`, leave the Activity Director
disabled, retain bounded scan/interaction/backoff defaults, and change no
unrelated setting. Start Login then Game through the accepted graceful
wrappers. Require healthy distinct PIDs, listeners on ports 1234, 1237, 1239,
1250, and 1280, Game API readiness, and PlayerBots startup with no startup
failure.

Use the `computer-use:computer-use` skill at the GUI boundary. Open the existing
registered AAEmu 1.2 launcher and leave authentication and gameplay to the
user. Do not enter or reveal credentials. After the user is present at the
Nuian starting area, use an existing eligible level-one Nuian identity. A
single explicit initial placement beside the user and normal bot admission are
allowed; the quest-intake controller itself must never teleport. Do not create
a character, edit/reset quest data, or issue any GM command that accepts,
advances, reports, or completes a quest.

Physical acceptance requires client-visible and server-authoritative proof
that the bot, starting idle beside the user, discovers the local giver from
nearby NPC data, prioritizes a story start over a nearer side quest, walks by
normal movement into interaction range, and accepts the story plus other
eligible starts on that chosen giver through AAEmu's normal non-forced quest
lifecycle. `/botdebug <characterId>`, structured server logs, and the visible
journal/state must agree. If no clean eligible identity exists or the zone has
no suitable story-plus-side layout, report the exact physical blocker; do not
fabricate state or substitute a different claim.

Record source/install/build/config/runtime identities and the physical result
in a new integration receipt and `HANDOFF.md`. Leave the interactive session
running for the user when healthy. Later cleanup must close client/launcher
normally, stop Game then Login gracefully, restore exact retained config bytes,
and verify zero relevant processes/listeners. Never force-stop, delete, reset,
or discard anything.
