# T-089 contract: integrate T-088 INCOMPLETE evidence

Independently verify candidate `7919a43fc370ea5f59edd77112385366f4928a94`,
exact parent `8abf3f3fd714680bf354a4fd2c1a6259b8171b8d`, and exactly the T-088 receipt
and handoff. Treat `D:\Codex-Labs\evidence\T-088\one-zone-autonomy-v2` as
immutable and read-only. Recompute every manifest relative path, byte length,
and SHA-256; require 18 payloads, 40,612 bytes, manifest SHA-256
`d2447105376fd7bfcbc24d77183f38afa16dc10806b32b656c9c7fb3079763ae`,
and zero missing, mismatched, duplicate, unsafe, or unlisted files.

Verify raw evidence, not only prose: exact binding/source/config fingerprints;
fresh-root creation; one raw zero-client sample; no derived/hash ledger row;
sampler exit code `2` caused by unavailable `Get-FileHash`; no retry; Login/Game
never started; config never edited and still hashes to
`7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202`;
zero observers, queries, fixtures, directed commands, waves, refills, or restart;
zero final Game/Login/client/observer/sampler processes, required listeners,
and live logs; unchanged observed MySQL PIDs `[6308,8076]` without database
access; clean reference/module; unchanged 30-entry host overlay. Preserve all
operator anomalies and unproven boundaries. Reject any attempted PASS upgrade.

If and only if green, replay the exact two candidate blobs onto the current
saved integration lineage, add one sanitized verification receipt and T-089
handoff, and fast-forward `integration/aaemu12-world` without rewriting history.
Do not start/control any process, use the registered host, mutate external
evidence, edit ledgers/lease, run tests/full suite, or repair the sampler.
