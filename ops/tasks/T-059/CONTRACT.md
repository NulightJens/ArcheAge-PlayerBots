# T-059 contract

## Outcome

The accepted T-058 qualified active-bot anchor is independently replayed onto
the current integration history, installed in the registered AAEmu 1.2 host,
and qualified by a clean full build and complete unit suite before a new live
headless physical attempt is authorized.

## Pass

- Start from exact integration head
  `06e57bfa427d79d7e96dd311dbb2a7716e64b5cf`. Verify T-058 candidate
  `5bd425327367b72871aef2eae7e51655a53f7bbc`, parent
  `fef939a460f262269f2586efd2e794fdcbe6f704`, declared six-file scope, and
  clean worktree.
- Independently replay only T-058's six files. Review the stable lowest-ID
  anchor selection, detached full-transform copy, same-world/instance and
  non-zero-zone checks, worldless fallback, controller freshness, and unchanged
  `spawnpassive` guard. Do not merge unrelated task/global-ledger state.
- Verify the pinned reference and registered integration host/module match the
  accepted T-056/T-057 identities before mutation. If mismatched beyond the
  documented installed state, stop under the contingency runbook rather than
  cleaning or substituting a checkout.
- Keep the active compatibility patch and all manifest hashes byte-identical.
  Prove the existing patch applies to `aaemu12_reference` and reverse-checks the
  installed host; do not reapply or regenerate it.
- Advance the installed module through the normal non-destructive Integrator
  commit/fast-forward workflow and record exact tested/installed head and tree.
- Prove installer idempotence, all 70 T-058 focused tests plus relevant module
  groups, a complete AAEmu 1.2 solution build, and one complete unit-suite
  invocation. Zero failures and zero build errors are required; report only
  intentional skips.
- Recheck clean reference, clean installed module, unchanged tracked host
  status, zero Login/Game/client processes, and free required ports.
- Commit only the replayed six files, sanitized integration receipt, and concise
  handoff with exact authorization for the next immutable live-headless attempt.

## Non-goals

Starting Login, GameServer, MySQL, or an ArcheAge client; claiming a runtime
lease; accessing schemas/databases; creating physical fixtures; changing the
compatibility patch/manifests/AAEmu 3.0; editing global ledgers or T-057
evidence; combat/stealth execution, scale, Population Director, soak, release.
