# T-098 handoff

Verdict: **FAIL**.

T-098 ran from binding commit `5d5919e56cd980d0db2ad1907178ba2a59868eb7`, dispatch base `bc6c8bd528e88d1bf2fe17b460336818cb58d85e`, task `01a05e8c-e6b0-7902-81b1-8dd7a6456dd3`, client token `client-new-thread:1bb59774-6513-4413-ab92-af3e3d8253cb`, and worktree `C:\Users\jensh\.codex\worktrees\4e59\PB-W00-control` under the exact committed `aaemu12` lease.

The exact source/config/fresh-root preflight passed. The reference was clean and detached at `62e3eb1d87da01194802ac886cd500134facad28`; the installed module was clean at source `761ffa1e0bd76d06532688f34b45e192a493b239`, tree `c36b1255cccb3a782e44e87a56bc9e867a946048`; and the compatibility patch, Game assembly, integrated client observer, and autonomy observer identities matched. The five required ports and live-log locations were empty, no AAEmu or ArcheAge client process existed, and OS-only observation found MySQL PIDs `6308` and `8076` without database access or control. The fresh v5 root was absent and created exactly once.

The integrated Windows PowerShell client-process observer launched once as PID `114064` before Login. Raw sample `000000` and ledger row zero were independently hash-validated at process count zero. Final replay validates 1,752 raw files and chained rows, zero client-process samples, zero errors, maximum adjacent gap `661.367 ms`, first sample `2026-09-01T20:06:16.6146797Z`, last sample `2026-09-01T20:22:32.8002491Z`, and terminal hash `b215a39a0c778d7b66ae11687112066512b5cbb023b4f5d77cbdaff593eaf449`. The observer spanned Login, runtime, and final cleanup, exited through its cooperative sentinel, and was never relaunched.

The deployed Director config changed only the nine contracted assignments. Original SHA-256 `7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202` became `5177e67c818d04ebf9c6b0164afc833e431cf4f687e660cffe02d5ef202de47c`, with initial delay `180000 ms`, then was restored byte-for-byte after shutdown.

Run one used Login PID `114656` and Game PID `116784`. The three unchanged autonomy observers launched once as PIDs `121484`, `76388`, and `119080`. Their first samples arrived `89367.568`, `89372.034`, and `89372.962 ms` after runtime-start evidence. Each produced two raw-verified offline samples with `online=false`, null object ID, and armed/live boundaries before admission. Final independent validation covers respectively 1,166/1,167/1,168 derived and transport samples and 1,157 raw files per bot, with every referenced raw length and hash green.

The corrected Director delay and zero-to-three bootstrap passed. Authoritative timestamps show start at `20:08:38.3319957Z` and first admission at `20:11:38.3873878Z`, an observed delay of `180055.392 ms` inside the contracted scheduler-tolerant `[180000,195000] ms` window. Admissions were `[20001,20002,20003]` at `20:11:38.3873878Z`, `20:11:53.4094248Z`, and `20:12:08.4527462Z`, with `15022.037` and `15043.321 ms` gaps, one per tick, maximum three, and zero failures, wrong-zone activations, or overlap.

At `20:19:12.1213194Z`, all bots were online, targetless, noncombat, stationary, idle, and at full resources. The exact first command, `spawnpassive 10004 5 20001`, was preserved byte-for-byte. The HTTP response rejected it: `Active bot anchor 20001 has an inconsistent world or instance boundary.` No NPC spawned. This is the first material invalid gate, so execution stopped without retry, later fixtures, directed gameplay, lifecycle wave, second wave, or restart. The safe-boundary record also retains the observed `12.163 m` horizontal distance between bot anchors 20001 and 20002; because no fixture spawned, fixture coordinates and required pairwise separations are unproven.

Game then Login stopped gracefully; the Director stopped once; all three bots despawned; and the once-only cleanup marker reports zero remaining bots and runtimes. The autonomy observers ended cooperatively. Run-one logs moved losslessly. Final checks prove zero runtime, autonomy-observer, client-observer, and ArcheAge client processes; zero required listeners and live logs; unchanged MySQL PIDs; exact source/config; and the expected 30-entry host overlay. The retained Game error log contains the native shutdown `PhysicsManager` disposed-service-provider warning; cleanup still passed.

The immutable evidence root is `D:\Codex-Labs\evidence\T-098\one-zone-autonomy-v5`. Its manifest covers 12,277 payloads and 33,980,701 bytes, length 2,507,986 bytes, with SHA-256 `1ab0074aad22af52abcef062982a10ad7c48146facabdde092e691c341127e42`. Independent validation found zero missing, mismatched, duplicate, unsafe, unlisted, or reparse-point payloads. Raw logs and credentials are not committed.

Retained procedural anomalies include fail-closed validation/launch probes, correction of a pre-command staging parser defect, cooperative cancellation of an extra-contractual 20 m anchor wait, and two fail-closed finalizer defects before sealing. PB-000's predictive-separation clarification arrived after the first fixture command and host rejection; the fixture did not spawn, and this timing is retained explicitly. None of these anomalies changed observer output or world state beyond the one rejected command.

Unproven boundaries are all fixture coordinates/separations, both autonomous lifecycle waves and refills, per-bot credits/progression/recovery/logout, distinct-PID restart/rebootstrap, two-minute dwell, scale, soak, packaging, client gameplay, database behavior, and AAEmu 3.0.

Integration action: independently verify the sealed root and integrate this commit only as the retained T-098 `FAIL` receipt. Keep T-037 blocked. PB-000 alone may release or reassign the lease after accepting cleanup; any successor requires a new task, exact binding/lease, and a new versioned evidence root.
