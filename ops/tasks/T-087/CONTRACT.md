# T-087 contract: integrate per-bot passive fixture anchors

## Outcome

The exact T-086 candidate is independently reviewed, replayed onto the saved
integration lineage, installed in the registered AAEmu 1.2 integration
checkout, and qualified by focused tests, a clean complete build, and exactly
one complete AAEmu 1.2 unit-suite invocation. A truthful receipt binds the
tested and installed source and decides whether a fresh one-zone Director
runtime proof may receive the lease.

## Authority and source identity

- Do not write the registered integration host/module or build outputs until
  PB-000 commits `ops/RUNTIME-LEASE.yaml` assigning `aaemu12` to T-087's exact
  thread for install/build/test only. This never authorizes Login, GameServer,
  MySQL, database, client, or live runtime control.
- Verify candidate `5c5d61c1f05c9419ba38e1cd247d35b6e8e92439`, exact parent
  `0da7166627f53118184687736132a88cdb668e1e`, clean detached writer worktree,
  and exactly the five T-086 paths declared in `TASK.yaml`. Preserve all
  Control Tower dispatch/binding/lease descendants; do not merge the writer
  worktree or rewrite history.
- Recompute patch SHA-256
  `baa0979a200eb7f4403985d7166092f937033c31650aa11336fba31543d96606`,
  verify both AAEmu 1.2 manifest declarations, and strict-apply-check it against
  clean pinned reference `62e3eb1d87da01194802ac886cd500134facad28`.

## Independent review and proof

- Review the implementation rather than trusting the handoff. Confirm the
  one/two-argument legacy path never evaluates `BotManager` and preserves its
  prior help, parsing, failure order, success bytes, terrain placement,
  passivity, displacement suppression, and no-respawn behavior.
- Confirm the exact optional third unsigned ID resolves only that active bot;
  rejects zero/unparseable, absent, mismatched, worldless, zone-zero,
  nonfinite, stale, and world/instance-inconsistent anchors before NPC
  creation; clones a detached transform; revalidates the same active bot before
  spawn; retains the selected world/zone/instance boundary; audits anchor ID,
  zone, and instance; and never assigns targets, mutates bot activity/combat/
  transform, or creates a gameplay control surface. Reject rather than repair
  any product defect.
- Replay candidate blobs byte-identically onto the current integration
  lineage. Advance the installed module non-destructively, run installer
  check-only before and after one idempotent normal install, and prove the
  read-only reference remains clean with no unreceipted tracked host changes
  outside the expected overlay.
- Run the exact 28-case `SpawnPassiveNpcCommandTests` focused selection, a clean
  complete AAEmu 1.2 build, and the full AAEmu 1.2 unit suite exactly once.
  Zero build or test failures are required. Record exact pass, intentional-skip,
  and warning counts. Do not consume a second full-suite invocation, even if
  the first is red.
- Verify zero Login/Game/client processes and free runtime ports before and
  after the gate. Leave MySQL wholly untouched: do not query, start, stop, or
  otherwise control it. Do not claim runtime behavior from build/tests.
- Commit only byte-identical candidate blobs/handoff, one sanitized integration
  receipt, and T-087 handoff. Fast-forward `integration/aaemu12-world` through
  the qualified commit without history rewrites and leave the worktree clean.

## Receipt and pass boundary

Bind integration parent, candidate/parent/tree, tested and installed module
source/tree, host/reference identities, patch hash, commands/counts,
warnings/failures, full-suite invocation count, no-runtime state, and unproven
boundaries. PASS may allow PB-000 to release the build-only lease and dispatch
a fresh versioned one-zone Director proof. It does not reinterpret retained
T-041, prove physical anchor placement or three-bot autonomy, or authorize
scale, soak, packaging, or release readiness.

## Non-goals

Product repair; runtime/gameplay/evidence generation; Login/Game/MySQL/database/
client control; roster mutation; global ledger/lease edits; retained evidence;
scale; soak; packaging; or AAEmu 3.0.
