# T-093 contract: integrate T-092 FAIL evidence

Independently verify candidate `885a6858e97ae073f86eed1aeef1c45bee3bb2ac`,
exact parent `dea1c660a076b2db1fbd5586214cfa2b9cde1d7b`, and exactly the T-092
receipt and handoff. Treat
`D:\Codex-Labs\evidence\T-092\one-zone-autonomy-v3` as immutable and
read-only. Recompute every manifest relative path, byte length, and SHA-256;
require 1,523 payloads, 22,604,049 bytes, manifest SHA-256
`39f6c85e69faa78d87824632f0db168bcbc87db4fd26b8d350f7bea70370809c`,
and zero missing, mismatched, duplicate, unsafe, or unlisted payloads.

Verify raw evidence, not only prose: exact binding, host, source, installed tree,
patch, Game assembly, config, schema-name, and integrated client-observer
fingerprints; fresh-root creation; one uninterrupted Windows PowerShell client
observer PID; 866 raw samples and 866 chained ledger rows; zero client-process
samples and errors; maximum adjacent gap `653.19` ms; terminal chain hash
`d65dcd4204332615de276dc8f0ea67f117f1b2012e732ef946c46db32439c664`;
first sample before Login/runtime start evidence and last sample after cleanup;
cooperative sentinel exit without relaunch.

Require Director admission order `[20001,20002,20003]` at
`18:02:07.7555867Z`, `18:02:22.7661669Z`, and `18:02:37.7786292Z`.
Independently prove the first autonomy samples were at approximately
`18:02:32Z`: bots `20001` and `20002` were already online, while only `20003`
had two offline samples before admission. Require this to remain a retained
FAIL with no retry, fixture commands, gameplay, waves, refills, or restart.
Verify graceful Game-then-Login shutdown, once-only cleanup, zero final bots,
runtimes, relevant processes, required listeners, and live logs; exact config
byte restoration; unchanged observed MySQL PIDs `[6308,8076]` without database
access; and all recorded anomalies and unproven boundaries. Reject any PASS
upgrade.

If and only if green, replay the exact two candidate blobs onto the current
saved integration lineage, add one sanitized verification receipt and T-093
handoff, and fast-forward `integration/aaemu12-world` without rewriting history.
Do not start or control any process, use the registered host, mutate external
evidence, edit global ledgers/lease, run tests/full suite, or repair or rerun
T-092.
