# T-109 contract: independently integrate retained T-108 INCOMPLETE

Act only as the evidence Integrator for T-108 candidate
`9a9a84532f321fbe6755bcf4957c41c8503086e5`, sole parent
`3c1329cf6e95a46998f889aa71cdd2673aa7ea4c`, tree
`01079a441274e1f999307d2b978768fe1c23bf72`, and stable patch ID
`8268d979f201ad25ef2e531fff6e5a7d911d8af2`. Start only from PB-000's
exact committed T-109 binding. Require exactly two candidate paths and blobs:

- `b27a7b46967848d10d957adc801fd2679e679196` T-108 receipt;
- `7853550e9a950ea9f0b38304b039c47284b6c639` T-108 handoff.

Replay those blobs byte-identically onto the committed saved lineage. Do not
merge the runtime worker branch, rewrite the evidence, or change source/tests.

Treat `D:\Codex-Labs\evidence\T-108\one-zone-autonomy-v7` as immutable and
read-only. Require manifest SHA-256
`ba96e8171f58bf1c0c4b600d5ae5d1c981aadb865d8e05ccf260e02f2e8a3430`
and terminal-result SHA-256
`3bba737efc1f047ec80e63e99f158cd823028c60170a20ee9b940173c2e6144c`.
Independently validate canonical relative-path safety and uniqueness, rehash
all 31 listed payloads and 86,107 bytes, reject reparse points, and require
zero missing, mismatched, duplicate, unsafe, or unlisted payloads. Do not write
an audit file into the root.

Decode the exact retained T-104 transforms from bit patterns
`1179957453/1179643699/-1065611208`,
`1179966362/1179652403/-1065603658`, and
`1179971072/1179656704/-1065577192`. Independently mirror the installed
single-precision angular placement operations and enumerate every tuple for
distances `{5,10,...,55}` and ordered angles
`{0,45,90,135,180,-135,-90,-45}`. Require 681,472 finite/in-range tuples,
427,142 with fixture pairwise separation at least 20 m, 142 satisfying the
literal row-oriented 3 m ownership gate, and zero satisfying both. Separately
recompute the diagnostic column orientation and require 4,386 ownership-valid
and 2,430 combined tuples. Require the retained extrema and prove that the
2,430 count belongs only to the column interpretation; do not weaken or reopen
the failed row-oriented gate.

Verify exact clean preflight and cleanup evidence: unchanged 1,772-byte
BotConfig SHA-256
`7b57a52149f6059cbcd972c38b161c46781d6730dbb3c6b93890171ae8c43202`,
zero Login/Game/client/observer launches, zero fixture or gameplay commands,
zero database/client access, zero processes/listeners/live logs, unchanged
observed MySQL PIDs `6308/8076`, exact clean reference/module/host overlay, and
no forced termination. Retain every recorded tooling and sealing anomaly.

After every independent gate passes, commit only the T-109 integration receipt
and handoff after the exact two-blob replay, then fast-forward saved branch
`integration/aaemu12-world` through exactly those two commits without history
rewrite. Report replay commit/tree, final saved head/tree, manifest and planner
recomputation, cleanup, retained anomalies, and unproven boundaries. Verdict
remains INCOMPLETE; T-037 stays blocked. Do not start runtime or prepare the v8
root. PB-000 alone may bind the fresh row-valid successor and runtime lease.
