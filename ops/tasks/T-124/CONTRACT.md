# T-124 contract: integrate and immediately test bot intelligence wave

Verify the exact four candidate identities from `TASK.yaml`, their ancestry,
clean writer worktrees, declared-path diffs, and retained focused/build proof.
Replay only those candidates onto the saved integration branch without
rewriting history. Preserve unrelated integration and control records.

Use one new retained build-only checkout at
`D:\Codex-Labs\aaemu-1.2-r208022-t124-integration-v1` rooted at the pinned
AAEmu 1.2 host commit. Apply the released v3 module patch, then the T-121
identity patch, T-122 doodad/quest patch, and T-123 transfer-road patch in that
order. If independent patches overlap, resolve the combined seam in repository
patch artifacts and prove both forward and reverse applicability; never edit
the registered reference or the running integration host.

Point the isolated checkout's module location at the merged integration source.
Run the merged identity, questing, and navigation focused fixtures immediately.
Require at least the retained 50 identity, 48 quest, and 30 navigation tests,
zero failures, and no zero-test invocation. Then run one no-incremental solution
build with zero errors. Only after those gates are green, run exactly one full
AAEmu 1.2 integration-wave suite invocation and retain its exact pass, skip,
failure, warning, duration, assembly, source, and patch fingerprints.

Do not stop, deploy to, configure, or otherwise mutate the healthy retained
T-120 runtime, its client, database, lease, or registered host. This task proves
the merged build quickly and creates the exact source/host candidate for a
separately leased immediate live smoke task. Record retained failures honestly,
write the integration receipt and `HANDOFF.md`, and commit the result.
