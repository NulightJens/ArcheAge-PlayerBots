# T-097 contract: integrate and install widened Director delay support

Independently verify T-096 candidate
`859e0daf64df482fcb17d83e16344387453a19b7` has sole parent
`1ebfd0ec47138ebf15268befde9ed1671aa05ef8` and exactly four changed paths:
the BotConfig source, BotConfig tests, T-096 receipt, and handoff. Require the
shared `300000` ms ceiling, preservation of `180000`, all six requested delay
boundary assertions, and `ToRuntimeConfiguration()` propagation to three
minutes. Preserve T-094 as retained FAIL.

After exact task/thread/worktree and build-only aaemu12 lease binding, replay
the four candidate blobs onto the current saved integration lineage. Verify
registered reference cleanliness and host base
`62e3eb1d87da01194802ac886cd500134facad28`. Require the unchanged AAEmu 1.2 v3
patch SHA-256
`baa0979a200eb7f4403985d7166092f937033c31650aa11336fba31543d96606`
and strict clean-apply identity. Install the exact replay source into the
registered integration module checkout without touching unrelated host overlay
paths. Record installed source commit/tree and Game assembly SHA-256.

Run the exact 32 BotConfig and 8 Director focused cases, then a clean
no-incremental build. Run exactly one complete AAEmu 1.2 unit suite for this
integration wave and require zero failures, preserving intentional skips and
warning counts. Do not run the full suite a second time. Never start Login,
Game, runtime observers, MySQL/database, or client; never edit deployed runtime
config, reference checkout, external evidence, global ledgers/lease, AAEmu 3.0,
scale, soak, or packaging.

If and only if every gate is green, add a sanitized integration receipt and
T-097 handoff, commit only the declared six paths, and fast-forward
`integration/aaemu12-world` without rewriting history. Report exact candidate,
replay, integration, installed-source/tree, patch, assembly, focused/build/full-
suite counts, retained warnings/skips, runtime state, unproven boundaries, and
the exact post-integration runtime action.
